import React from 'react';
import { Column, Text } from '@/components';

export const MediaProcessorScreen: React.FC = () => {
  return (
    <Column flex={1} justifyCenter alignCenter>
      <Text>{'Hello world'}</Text>
    </Column>
  );
};
